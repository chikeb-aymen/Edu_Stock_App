server:
  port: 8083

spring:
  application:
    name: BILLING-SERVICE
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/stock_billing
    username: postgres
    password: aymen

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka